{
  "name": "turf-monster",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:ui": "npx playwright test --ui",
    "test:smoke": "npx playwright test --grep @smoke",
    "test:comprehensive": "npx playwright test --grep-invert @smoke",
    "test:parallel": "bin/e2e-parallel",
    "test:smoke:parallel": "bin/e2e-parallel -- --grep @smoke",
    "market-snapshot": "node scripts/scrape_draftkings.js",
    "scrape": "node scripts/scrape_draftkings.js",
    "scrape:headed": "node scripts/scrape_draftkings.js --headed",
    "test:scrape": "node --test scripts/"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "tweetnacl": "^1.0.3"
  },
  "dependencies": {
    "tailwindcss": "^4.3.1"
  }
}
